{{#if success_msg}}
    <div class="alert alert-success">{{success_msg}}</div> <!-- available because we set res.locals.success_msg -->
{{/if}}

{{#if error_msg}}
    <div class="alert alert-danger">{{error_msg}}</div> <!-- available because we set res.locals.error_msg -->
{{/if}}